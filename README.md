# Clicker

Clicker is an incremental game about harvesting energy from the Lumen Core, building momentum, and buying stronger taps and automation.

To test locally, run:

```sh
python3 -m http.server --bind 0.0.0.0 8005
```

Then open http://localhost:8005.
